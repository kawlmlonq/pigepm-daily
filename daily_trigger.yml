name: Daily Pigepm Trigger

on:
  schedule:
    - cron: '*/5 * * * *'  # 每 5 分鐘（UTC） 
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Hugging Face Space
        run: curl -X POST https://kawlmlonq--pigepm-colab-autorun.hf.space/run

